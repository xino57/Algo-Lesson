/*
Cours tableau 
créer un tableau 
let tableau=[];
let liste =["fromage,"fruit"];
afficher (liste[1]); Permet d'afficher "fruit" avec la postion 1

Rappel:
Parcourir un tableau 
Ne pas oublier de déclarer le tableau avec le varaible
Pour le pas du tableau 
for (let i=0; i<taille(liste); i++){
    afficher (liste[i])
}

faire la somme des valeurs dans le tableau 
let liste=[1,2,3,4];
let somme=0;
somme = somme+liste[i];
afficher(somme);

dans le cas d'un produit 
let produit = 1;
produit = produit*liste[i];
afficher(produit)
1*(1*2*3*4)

dans le cas d'une concaténatiion (chaine de caractère)
let chaine=""
chaine=chaine+lsit[i];
afficher(chaine)

ajouter une case dans un tableau (sans boucle)
let liste=["fromage", "fruit"]
liste [2]="légume";

tableau embriqué 
le parcourir sans bloucle 
let lsite=[
    ["gruyère", "paramesan"]
    ["pommes", "poires", "banane"],
    ["habricots"]
]
afficher(liste[0][1]); = "parmesan"

Parcourir un tableau à deux dimensions avec boucle (for)
for(leti=0;i<taille(liste); i++){
    for(leti=0,j<taille(liste[i]); j++){
        afficher(lsite[i][j]);
    }
}

les fonctions 

fucntion solde(prix, remise){
    let result;
    result = prix * (1-0.1);
    return result;
}
afficher(solde)(50,0.5));
afficher(solde(10,0.25));

let cout = 100;
let reduc =0.2
afficher(solde(cout, reduc));

Boucle while 
let nb=lire("Entrez un nombre en tro 0 et 10 inclus");
while (nb<10 nb>10){
    nb=lire("Non, un nombre entre 0 et 10 inclus")
}
afficher(nb);

*/



